<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="mybuttonSytle">
        <item name="android:textColor">#ffff0000</item>
        <item name="myButtonshow">true</item>
        <item name="labelPosition">right</item>
    </style>

    <!-- Base application theme. -->
    <style name="ToolbarTheme" parent="@android:style/Theme.Material.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!-- Base application theme. -->
    <style name="orignToolbarTheme" parent="@android:style/Theme.Material.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
</resources>